import { MoveElementDirective } from './move-element.directive';

describe('MoveElementDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveElementDirective();
    expect(directive).toBeTruthy();
  });
});
